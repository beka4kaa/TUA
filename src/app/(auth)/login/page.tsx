import { LoginForm } from "@/components/auth/login-form";

export const metadata = {
    title: "Login | Stockermans",
    description: "Sign in to your Stockermans account",
};

export default function LoginPage() {
    return <LoginForm />;
}
